# Unit tests only exercise pure functions; tests/conftest.py stubs the
# homeassistant imports when the real package is unavailable (e.g. on Python
# versions Home Assistant doesn't support yet).
pytest
ayla_iot_unofficial==1.5.0
